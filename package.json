{
  "name": "hqs-online",
  "version": "1.0.0",
  "description": "Front-end static site + db.json for JSON Server (HQs Online)",
  "scripts": {
    "start:api": "json-server --watch db/db.json --port 3000",
    "start:static": "npx serve public -l 8080",
    "start:all": "echo 'Abra dois terminais: npm run start:api e npm run start:static'"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  },
  "keywords": [],
  "author": "Gabriel Henrique",
  "license": "MIT"
}
